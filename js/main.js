// js/main.js

// Configuración de colores de Tailwind (Idealmente esto va en tailwind.config.js, 
// pero lo mantengo aquí por la estructura CDN del proyecto)
tailwind.config = {
    theme: {
        extend: {
            colors: {
                'primary-blue': '#5C61D0', 
                'accent-red': '#EB4336', 
                'text-dark': '#1E1E1E', 
                'text-light': '#6b7280', 
            }
        }
    }
}
